# Day 28：发布准备 + 最终检查
**日期：** 2026-06-04
**状态：** pending
**依赖：** Day 27（验收通过）

---

## 一、生产环境配置

### 1.1 前端构建优化
- [ ] 配置 `vite.config.ts` 生产模式：
  - 代码分割（vendor chunk）
  - 资源压缩
  - 输出到 `frontend/dist/`
- [ ] 构建并验证产物大小合理（< 1MB gzipped）

### 1.2 后端生产配置
- [ ] 关闭 DEBUG 模式
- [ ] 配置 CORS 白名单
- [ ] 确认 `.env` 与 `.env.example` 一致
- [ ] 添加健康检查端点 `GET /api/health`

### 1.3 部署文档
- [ ] 创建 `docs/deploy.md`：
  - 环境要求（Python 3.10+, Node 18+）
  - 后端部署（uvicorn + systemd 或 Docker）
  - 前端部署（nginx 静态文件或 Vite preview）
  - Docker Compose 一键部署方案（可选）

---

## 二、最终检查

### 2.1 功能完整性
- [ ] 重新走一遍 Day 26 的联调 checklist（12 步）
- [ ] 确认所有 P0 bug 已修复
- [ ] 确认所有 P1 bug 已记录或修复

### 2.2 代码质量
- [ ] `tsc --noEmit` 零错误
- [ ] `python -m pytest` 178 通过
- [ ] 前端测试全部通过
- [ ] 无 console.log 残留（仅保留必要的错误日志）
- [ ] 无 `TODO` / `FIXME` 残留（或已记录到计划中）

### 2.3 UI 一致性
- [ ] 三个页面风格统一（暖色、直角、Trebuchet MS + PingFang SC）
- [ ] 字体大小层次分明
- [ ] 间距一致
- [ ] 按钮样式统一（主按钮/次按钮/幽灵按钮）

### 2.4 安全性
- [ ] 文件上传限制（仅 .xlsx，最大 50MB）
- [ ] API 无明显安全漏洞（SQL 注入、路径遍历等）
- [ ] 错误信息不暴露内部路径

---

## 三、Git 整理

- [ ] 确认 `.gitignore` 完整：
  ```
  node_modules/
  __pycache__/
  *.pyc
  .env
  frontend/dist/
  uploads/
  data/
  logs/
  *.bak
  _*.py
  ```
- [ ] 确保没有敏感信息（API key 等）被提交
- [ ] 分支整理：合并到 main
- [ ] 打 tag：`v1.0.0`

---

## 四、检查清单

- [ ] 生产构建成功
- [ ] 部署文档就位
- [ ] 全流程 12 步再次通过
- [ ] 所有测试通过
- [ ] 代码干净无遗留问题
- [ ] Git tag v1.0.0
