# Day 22.4：补齐遗漏

**日期：** 2026-05-29
**状态：** pending
**依赖：** 无（与 22.2/22.3 可并行）

---

## 目标

修复全面排查中发现的 4 个遗漏项。

---

## 修复清单

### 1. chart_specs 表缺少 Alembic 迁移
- 新增 `alembic/versions/20260529_0009_add_chart_specs.py`
- 字段：id, task_id(FK), chart_index, chart_type, title, x_field, y_fields_json, series_json, highlights_json, source_cells_json, reason, created_at

### 2. TaskRecordModel 缺少 relationship
- 补充 `chart_specs`、`insight_records`、`summary_records` 的 relationship 映射

### 3. .env.example 缺少 LLM 配置
- 补充 `FORMULA_LLM_API_URL`、`FORMULA_LLM_API_KEY`、`FORMULA_LLM_MODEL`
- 补充 `ANALYSIS_LLM_API_URL`、`ANALYSIS_LLM_API_KEY`、`ANALYSIS_LLM_MODEL`

### 4. TaskReviewPage 英文文案中文化
- "Day 8 Gate 1 Frame" → 中文标题
- "Structure Review Studio" → 中文
- 所有按钮和标签中文化

---

## 文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `alembic/versions/20260529_0009_add_chart_specs.py` | NEW | chart_specs 迁移 |
| `app/db/models/task_record.py` | MOD | 补充 relationship |
| `.env.example` | MOD | 补充 LLM 配置 |
| `frontend/src/pages/TaskReviewPage.tsx` | MOD | 中文化 |